# Issue assets

Figures embedded in GitHub issues. Kept on this orphan branch, which shares no
history with `main`, so the source tree keeps its no-tracked-PNG rule
(`.gitignore: *.png`) while issues can still carry evidence images.

- `issues/610/` — cloudef m7 cross-filter anchor per-tile map (issue #610)
